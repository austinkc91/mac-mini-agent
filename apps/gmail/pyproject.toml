[project]
name = "gmail"
version = "0.1.0"
description = "Gmail CLI tool for autonomous email management"
requires-python = ">=3.11"
dependencies = [
    "click>=8.0",
]

[tool.uv]
dev-dependencies = []
