[project]
name = "drive-cli"
version = "0.1.0"
description = "Terminal automation CLI for AI agents on macOS"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1",
    "psutil>=5.9",
]

[project.scripts]
drive = "main:cli"
